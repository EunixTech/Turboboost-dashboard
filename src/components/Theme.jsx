
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#ffffff', // Replace with your desired default background color
      paper: '#f8f8f8',   // Replace with your desired paper background color
      // Add other color variants as needed
    },
    // Add other palette properties as needed
  },
  // Add other theme properties as needed
});

export default theme;
